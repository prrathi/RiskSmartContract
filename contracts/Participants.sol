pragma solidity >=0.5.17;
import "./Platforms.sol";

contract Participant{
    uint idx;    
    uint32 accountVal;
    uint32 premium;
}
contract ParticipantFuncs is Platform {
    function recordClaim(uint loss, Participant p) {
     
      
  } 

        function getPaidPremium() return (uint32, P, {
        
    }
    function getIncurredLosses() return (uint32);
}